    using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Datos;

namespace Negocio
{
    public class Actividad
    {
        public static Entities context()
        {
            return Datos.Context.context();
        }

        public Exception Insertar(Datos.Actividad actividad)
        {
            try
            {
                context().Actividad.AddObject(actividad);
                context().SaveChanges();
            }
            catch (Exception ex)
            {
                return ex;
            }
            return null;
        }

        //Falta implementar el Modificar
        public Exception Modificar(Datos.Actividad actividad)  
        {
            return null;
        }

        //Esto es un seleccionar todo para las actividades
        public static IEnumerable<Datos.Actividad> SeleccionarTodo() { 
            IEnumerable<Datos.Actividad> listaActividad = context().Actividad.Where(p => p.estado != 0);
            return listaActividad;
        }

        //Busca una actividad por ID actividad
        public static Datos.Actividad BuscarId(short id)
        {
            return context().Actividad.FirstOrDefault(p => p.id == id);
        }

        //Busca una actividad por Nombre de actividad
        public static IEnumerable<Datos.Actividad> BuscarNombre(String nombre)
        {
            return context().Actividad.Where(p => p.nombre == nombre);
        }

        //falta probar, podria no funcionar correctamente (me copie de tavo)
        public static void Eliminar(Datos.Actividad actividad)
        {
            context().Evento.SingleOrDefault(e => e.id == actividad.id).estado=0;
            context().SaveChanges();
        }
    }
}
